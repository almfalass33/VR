name: blank

on:
  push:

jobs:
  era:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        strain: ["congress", "curse", "weep", "meter", "startle"]
        sew: ["cubic", "diverse", "impressive", "discipline"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar yY
